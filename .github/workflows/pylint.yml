# Este workflow do Pylint foi desativado pelo usuário.
# O pipeline ativo é o ci.yml (Integração Contínua & Segurança).
name: Pylint (Desativado)

on:
  push:
    branches: [ none ]

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Informar desativação
        run: echo "Pylint desativado. Utilizando apenas o ci.yml."
